const DARK_MODE_CLASS_NAME = "dark";

enum Tables {
    messages = "messages",
}

enum Models {
    mistral = "mistralai/mistral-7b-instruct",
}

export {
    DARK_MODE_CLASS_NAME,
    Tables,
    Models,
}
